name: test secret
on:
  workflow_dispatch:

jobs:
 test1:
    runs-on: ubuntu-latest
    steps:
      - name: Call secret
        run: echo my secret is ${{ secrets.MYTOKEN1}}
  
 test2:
    runs-on: ubuntu-latest
    env:
      MYSECRET: ${{ MYTOKEN1 }}
    steps:
      - name: call 2 
        run: echo my secret again $MYSECRET

